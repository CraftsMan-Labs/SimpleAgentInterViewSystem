services:
  simpleagent-interview-system:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: simpleagent-interview-system
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "8091:8091"
    restart: unless-stopped
